{
	"name": "demo",
	"description": "Demo for Laravel Framework.",
	"keywords": [],
	"license": "GPL 2.0",
	"type": "project",
	"require": {
		
	},
	"require-dev": {
		
	},
	"autoload": {
		"classmap": [
			"database"
		],
		"psr-4": {
			"App\\": "app/"
		}
	},
	"autoload-dev": {
		"classmap": [
			"tests/TestCase.php"
		]
	},
	"scripts": {
		"post-install-cmd": [
			"php artisan clear-compiled",
			"php artisan optimize-separated"
		],
		"post-update-cmd": [
			"php artisan clear-compiled",
			"php artisan optimize-separated"
		],
		"post-create-project-cmd": [
			"php -r \"copy('.env.example', '.env');\"",
			"php artisan key:generate"
		]
	},
	"config": {
		"preferred-install": "dist"
	}
}
